================================================================================
TEMP → PERMANENT PASSWORD — BACKEND IMPLEMENTED
Date:       2026-08-07
Status:     DONE (BE portion of invite password plan)
FE still needs: change-password page + route guard on mustChangePassword
================================================================================


1. WHAT WAS IMPLEMENTED
--------------------------------------------------------------------------------

  ✓ mustChangePassword on login / refresh / switch-company payloads
  ✓ mustChangePassword on GET /auth/me
  ✓ POST /api/v1/auth/change-password
  ✓ Soft API gate: other APIs return 403 MUST_CHANGE_PASSWORD while flag true
  ✓ Password history row saved on change
  ✓ mustChangePassword cleared + cache invalidated after change


2. AUTH RESPONSE (login / me / refresh)
--------------------------------------------------------------------------------

  {
    ...
    "permissions": [...],
    "tenantAdmin": false,
    "platformOwner": false,
    "mustChangePassword": true
  }


3. CHANGE PASSWORD
--------------------------------------------------------------------------------

  POST /api/v1/auth/change-password
  Auth: Bearer JWT

  Body:
  {
    "currentPassword": "TempPass9xK2",
    "newPassword": "MyNewPass1",
    "confirmPassword": "MyNewPass1"
  }

  Success data:
  { "mustChangePassword": false }

  Errors:
    401 — current password wrong
    400 — weak / mismatch / same as current


4. API GATE (while mustChangePassword=true)
--------------------------------------------------------------------------------

  Allowed:
    GET  /auth/me
    GET  /auth/companies
    POST /auth/change-password
    POST /auth/logout
    POST /auth/switch-company
    POST /auth/refresh (public)
    Public routes

  Everything else → 403
  {
    "success": false,
    "message": "Password change required",
    "code": "MUST_CHANGE_PASSWORD"
  }


5. FE NEXT
--------------------------------------------------------------------------------

  1. Store mustChangePassword from login + /me
  2. If true → force /auth/change-password route
  3. Call POST /auth/change-password
  4. On success clear flag → dashboard

================================================================================
END
================================================================================
