# Application
NODE_ENV=development
PORT=3000
API_PREFIX=api/v1
APP_NAME=ERP Backend

# CORS (frontend dev origin — required for httpOnly refresh cookie)
CORS_ORIGIN=http://localhost:3001

# Database
# Local PostgreSQL (Docker):
DATABASE_URL=postgresql://erp:erp_secret@localhost:5432/erp_db?schema=public
DIRECT_DATABASE_URL=postgresql://erp:erp_secret@localhost:5432/erp_db?schema=public

# Neon Cloud PostgreSQL (see NEON_DATABASE_GUIDE.txt):
# DATABASE_URL="postgresql://USER:PASS@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"
# DIRECT_DATABASE_URL="postgresql://USER:PASS@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Permission cache (seconds)
PERMISSION_CACHE_TTL_SECONDS=300

# OAuth — Google
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# OAuth — Microsoft (Outlook / Azure AD)
MICROSOFT_OAUTH_CLIENT_ID=
MICROSOFT_OAUTH_CLIENT_SECRET=
MICROSOFT_OAUTH_TENANT_ID=common

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
USE_MEMORY_SESSION=true

# BullMQ
BULLMQ_PREFIX=erp

# Google Cloud Storage
GCS_PROJECT_ID=your-gcp-project-id
GCS_BUCKET=your-erp-bucket
GCS_KEY_FILE_PATH=./gcp-service-account.json
GCS_PUBLIC_BASE_URL=
