meta {
  name: 04 Get My Companies
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/auth/companies
  body: none
  auth: none
}

script:post-response {
  const auth = require('../../lib/auth.js');
  auth.assertSuccess(res);
}

docs {
  Lists all companies user can access. Token auto-attached.
}
